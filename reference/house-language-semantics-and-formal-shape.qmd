---
title: "Language, Semantics, and Formal Shape"
---

# Language, Semantics, and Formal Shape

Range: `0113-0249`.

## Completion

| Status   | Count |
| -------- | ----- |
| authored | 137   |

Use the summary table for scanning. Each row links to the detailed anchored entry below.

| Sigil | Term                                   | Completion | Semantic        | Summary                                                                                                                                                                                                                        |
| ----- | -------------------------------------- | ---------- | --------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 0113  | [abstract class](#rune-0113)           | authored   | generated_draft | `abstract class` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the abstract class obligation named before work proceeds.                     |
| 0114  | [alias](#rune-0114)                    | authored   | generated_draft | `alias` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the alias obligation named before work proceeds.                                       |
| 0115  | [annotation](#rune-0115)               | authored   | generated_draft | `annotation` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the annotation obligation named before work proceeds.                             |
| 0116  | [argument](#rune-0116)                 | authored   | generated_draft | `argument` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the argument obligation named before work proceeds.                                 |
| 0117  | [arity](#rune-0117)                    | authored   | generated_draft | `arity` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the arity obligation named before work proceeds.                                       |
| 0118  | [assembly](#rune-0118)                 | authored   | generated_draft | `assembly` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the assembly obligation named before work proceeds.                                 |
| 0119  | [assertion {Lang}](#rune-0119)         | authored   | generated_draft | `assertion` in its Lang sense is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the assertion obligation named before work proceeds.             |
| 0120  | [AST](#rune-0120)                      | authored   | reviewed        | The tree form of parsed program structure.                                                                                                                                                                                     |
| 0121  | [attribute](#rune-0121)                | authored   | generated_draft | `attribute` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the attribute obligation named before work proceeds.                               |
| 0122  | [binding](#rune-0122)                  | authored   | generated_draft | `binding` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the binding obligation named before work proceeds.                                   |
| 0123  | [borrow {Lang}](#rune-0123)            | authored   | generated_draft | `borrow` in its Lang sense is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the borrow obligation named before work proceeds.                   |
| 0124  | [borrow checker](#rune-0124)           | authored   | reviewed        | A checker that enforces safe borrowing and lifetime law before runtime.                                                                                                                                                        |
| 0125  | [bytecode](#rune-0125)                 | authored   | generated_draft | `bytecode` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the bytecode obligation named before work proceeds.                                 |
| 0126  | [capture](#rune-0126)                  | authored   | generated_draft | `capture` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the capture obligation named before work proceeds.                                   |
| 0127  | [cast](#rune-0127)                     | authored   | generated_draft | `cast` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the cast obligation named before work proceeds.                                         |
| 0128  | [class](#rune-0128)                    | authored   | generated_draft | `class` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the class obligation named before work proceeds.                                       |
| 0129  | [closure](#rune-0129)                  | authored   | generated_draft | `closure` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the closure obligation named before work proceeds.                                   |
| 0130  | [coercion](#rune-0130)                 | authored   | generated_draft | `coercion` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the coercion obligation named before work proceeds.                                 |
| 0131  | [comment](#rune-0131)                  | authored   | generated_draft | `comment` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the comment obligation named before work proceeds.                                   |
| 0132  | [compile-time](#rune-0132)             | authored   | reviewed        | Known and fixed before execution begins.                                                                                                                                                                                       |
| 0133  | [compiler](#rune-0133)                 | authored   | reviewed        | The translator from source language to lower-level executable form.                                                                                                                                                            |
| 0134  | [constant](#rune-0134)                 | authored   | generated_draft | `constant` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the constant obligation named before work proceeds.                                 |
| 0135  | [constraint {Lang}](#rune-0135)        | authored   | generated_draft | `constraint` in its Lang sense is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the constraint obligation named before work proceeds.           |
| 0136  | [constructor](#rune-0136)              | authored   | generated_draft | `constructor` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the constructor obligation named before work proceeds.                           |
| 0137  | [contravariance](#rune-0137)           | authored   | generated_draft | `contravariance` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the contravariance obligation named before work proceeds.                     |
| 0138  | [covariance](#rune-0138)               | authored   | generated_draft | `covariance` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the covariance obligation named before work proceeds.                             |
| 0139  | [currying](#rune-0139)                 | authored   | generated_draft | `currying` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the currying obligation named before work proceeds.                                 |
| 0140  | [declaration](#rune-0140)              | authored   | generated_draft | `declaration` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the declaration obligation named before work proceeds.                           |
| 0141  | [decorator {Lang}](#rune-0141)         | authored   | generated_draft | `decorator` in its Lang sense is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the decorator obligation named before work proceeds.             |
| 0142  | [default](#rune-0142)                  | authored   | generated_draft | `default` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the default obligation named before work proceeds.                                   |
| 0143  | [definition](#rune-0143)               | authored   | generated_draft | `definition` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the definition obligation named before work proceeds.                             |
| 0144  | [denotation](#rune-0144)               | authored   | generated_draft | `denotation` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the denotation obligation named before work proceeds.                             |
| 0145  | [destructor {Lang}](#rune-0145)        | authored   | generated_draft | `destructor` in its Lang sense is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the destructor obligation named before work proceeds.           |
| 0146  | [dispatch {Lang}](#rune-0146)          | authored   | generated_draft | `dispatch` in its Lang sense is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the dispatch obligation named before work proceeds.               |
| 0147  | [domain-specific language](#rune-0147) | authored   | generated_draft | `domain-specific language` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the domain-specific language obligation named before work proceeds. |
| 0148  | [dynamic scope](#rune-0148)            | authored   | generated_draft | `dynamic scope` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the dynamic scope obligation named before work proceeds.                       |
| 0149  | [effect](#rune-0149)                   | authored   | generated_draft | `effect` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the effect obligation named before work proceeds.                                     |
| 0150  | [enum](#rune-0150)                     | authored   | generated_draft | `enum` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the enum obligation named before work proceeds.                                         |
| 0151  | [evaluation {Lang}](#rune-0151)        | authored   | generated_draft | `evaluation` in its Lang sense is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the evaluation obligation named before work proceeds.           |
| 0152  | [export {Lang}](#rune-0152)            | authored   | generated_draft | `export` in its Lang sense is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the export obligation named before work proceeds.                   |
| 0153  | [expression](#rune-0153)               | authored   | generated_draft | `expression` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the expression obligation named before work proceeds.                             |
| 0154  | [extern](#rune-0154)                   | authored   | generated_draft | `extern` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the extern obligation named before work proceeds.                                     |
| 0155  | [field {Lang}](#rune-0155)             | authored   | generated_draft | `field` in its Lang sense is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the field obligation named before work proceeds.                     |
| 0156  | [final](#rune-0156)                    | authored   | generated_draft | `final` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the final obligation named before work proceeds.                                       |
| 0157  | [function](#rune-0157)                 | authored   | generated_draft | `function` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the function obligation named before work proceeds.                                 |
| 0158  | [generic {Lang}](#rune-0158)           | authored   | reviewed        | One definition parameterized over many concrete types or cases.                                                                                                                                                                |
| 0159  | [grammar](#rune-0159)                  | authored   | reviewed        | The rules that say what strings count as well-formed structure.                                                                                                                                                                |
| 0160  | [higher-order function](#rune-0160)    | authored   | generated_draft | `higher-order function` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the higher-order function obligation named before work proceeds.       |
| 0161  | [identifier](#rune-0161)               | authored   | generated_draft | `identifier` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the identifier obligation named before work proceeds.                             |
| 0162  | [impl](#rune-0162)                     | authored   | generated_draft | `impl` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the impl obligation named before work proceeds.                                         |
| 0163  | [implementation {Lang}](#rune-0163)    | authored   | generated_draft | `implementation` in its Lang sense is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the implementation obligation named before work proceeds.   |
| 0164  | [import](#rune-0164)                   | authored   | generated_draft | `import` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the import obligation named before work proceeds.                                     |
| 0165  | [include](#rune-0165)                  | authored   | generated_draft | `include` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the include obligation named before work proceeds.                                   |
| 0166  | [inference](#rune-0166)                | authored   | reviewed        | Recovering missing type or shape information from surrounding evidence.                                                                                                                                                        |
| 0167  | [inheritance](#rune-0167)              | authored   | generated_draft | `inheritance` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the inheritance obligation named before work proceeds.                           |
| 0168  | [inline {Lang}](#rune-0168)            | authored   | generated_draft | `inline` in its Lang sense is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the inline obligation named before work proceeds.                   |
| 0169  | [instruction {Lang}](#rune-0169)       | authored   | generated_draft | `instruction` in its Lang sense is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the instruction obligation named before work proceeds.         |
| 0170  | [interface {Lang}](#rune-0170)         | authored   | generated_draft | `interface` in its Lang sense is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the interface obligation named before work proceeds.             |
| 0171  | [interpreter {Lang}](#rune-0171)       | authored   | reviewed        | A runtime that executes a representation rather than precompiling it away.                                                                                                                                                     |
| 0172  | [IR](#rune-0172)                       | authored   | reviewed        | An internal representation built for analysis and transformation.                                                                                                                                                              |
| 0173  | [keyword](#rune-0173)                  | authored   | generated_draft | `keyword` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the keyword obligation named before work proceeds.                                   |
| 0174  | [lambda](#rune-0174)                   | authored   | generated_draft | `lambda` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the lambda obligation named before work proceeds.                                     |
| 0175  | [lexical scope](#rune-0175)            | authored   | generated_draft | `lexical scope` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the lexical scope obligation named before work proceeds.                       |
| 0176  | [lifetime {Lang}](#rune-0176)          | authored   | generated_draft | `lifetime` in its Lang sense is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the lifetime obligation named before work proceeds.               |
| 0177  | [linker](#rune-0177)                   | authored   | reviewed        | The binder that turns compiled pieces into one loadable whole.                                                                                                                                                                 |
| 0178  | [literal](#rune-0178)                  | authored   | generated_draft | `literal` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the literal obligation named before work proceeds.                                   |
| 0179  | [loader](#rune-0179)                   | authored   | generated_draft | `loader` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the loader obligation named before work proceeds.                                     |
| 0180  | [macro](#rune-0180)                    | authored   | reviewed        | Code that writes or reshapes code.                                                                                                                                                                                             |
| 0181  | [metaprogramming](#rune-0181)          | authored   | generated_draft | `metaprogramming` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the metaprogramming obligation named before work proceeds.                   |
| 0182  | [method](#rune-0182)                   | authored   | generated_draft | `method` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the method obligation named before work proceeds.                                     |
| 0183  | [mixin](#rune-0183)                    | authored   | generated_draft | `mixin` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the mixin obligation named before work proceeds.                                       |
| 0184  | [module {Lang}](#rune-0184)            | authored   | generated_draft | `module` in its Lang sense is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the module obligation named before work proceeds.                   |
| 0185  | [monomorphization](#rune-0185)         | authored   | generated_draft | `monomorphization` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the monomorphization obligation named before work proceeds.                 |
| 0186  | [mutability](#rune-0186)               | authored   | generated_draft | `mutability` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the mutability obligation named before work proceeds.                             |
| 0187  | [name resolution](#rune-0187)          | authored   | generated_draft | `name resolution` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the name resolution obligation named before work proceeds.                   |
| 0188  | [namespace {Lang}](#rune-0188)         | authored   | reviewed        | A naming region that prevents collisions and clarifies ownership.                                                                                                                                                              |
| 0189  | [object](#rune-0189)                   | authored   | generated_draft | `object` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the object obligation named before work proceeds.                                     |
| 0190  | [opcode {Lang}](#rune-0190)            | authored   | generated_draft | `opcode` in its Lang sense is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the opcode obligation named before work proceeds.                   |
| 0191  | [overload](#rune-0191)                 | authored   | generated_draft | `overload` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the overload obligation named before work proceeds.                                 |
| 0192  | [override](#rune-0192)                 | authored   | generated_draft | `override` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the override obligation named before work proceeds.                                 |
| 0193  | [ownership](#rune-0193)                | authored   | generated_draft | `ownership` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the ownership obligation named before work proceeds.                               |
| 0194  | [package {Lang}](#rune-0194)           | authored   | generated_draft | `package` in its Lang sense is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the package obligation named before work proceeds.                 |
| 0195  | [parameter](#rune-0195)                | authored   | generated_draft | `parameter` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the parameter obligation named before work proceeds.                               |
| 0196  | [parametric polymorphism](#rune-0196)  | authored   | generated_draft | `parametric polymorphism` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the parametric polymorphism obligation named before work proceeds.   |
| 0197  | [parser](#rune-0197)                   | authored   | reviewed        | The mechanism that turns surface text into structured meaning.                                                                                                                                                                 |
| 0198  | [pattern](#rune-0198)                  | authored   | generated_draft | `pattern` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the pattern obligation named before work proceeds.                                   |
| 0199  | [pattern matching](#rune-0199)         | authored   | generated_draft | `pattern matching` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the pattern matching obligation named before work proceeds.                 |
| 0200  | [polymorphism](#rune-0200)             | authored   | reviewed        | One interface, many concrete behaviors.                                                                                                                                                                                        |
| 0201  | [procedure {Lang}](#rune-0201)         | authored   | generated_draft | `procedure` in its Lang sense is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the procedure obligation named before work proceeds.             |
| 0202  | [program](#rune-0202)                  | authored   | generated_draft | `program` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the program obligation named before work proceeds.                                   |
| 0203  | [property {Lang}](#rune-0203)          | authored   | generated_draft | `property` in its Lang sense is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the property obligation named before work proceeds.               |
| 0204  | [protected](#rune-0204)                | authored   | generated_draft | `protected` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the protected obligation named before work proceeds.                               |
| 0205  | [public](#rune-0205)                   | authored   | generated_draft | `public` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the public obligation named before work proceeds.                                     |
| 0206  | [purity](#rune-0206)                   | authored   | generated_draft | `purity` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the purity obligation named before work proceeds.                                     |
| 0207  | [qualified name](#rune-0207)           | authored   | generated_draft | `qualified name` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the qualified name obligation named before work proceeds.                     |
| 0208  | [readonly](#rune-0208)                 | authored   | generated_draft | `readonly` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the readonly obligation named before work proceeds.                                 |
| 0209  | [record {Lang}](#rune-0209)            | authored   | generated_draft | `record` in its Lang sense is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the record obligation named before work proceeds.                   |
| 0210  | [reference {Lang}](#rune-0210)         | authored   | generated_draft | `reference` in its Lang sense is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the reference obligation named before work proceeds.             |
| 0211  | [referential transparency](#rune-0211) | authored   | generated_draft | `referential transparency` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the referential transparency obligation named before work proceeds. |
| 0212  | [reflection](#rune-0212)               | authored   | generated_draft | `reflection` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the reflection obligation named before work proceeds.                             |
| 0213  | [resolution](#rune-0213)               | authored   | generated_draft | `resolution` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the resolution obligation named before work proceeds.                             |
| 0214  | [return type](#rune-0214)              | authored   | generated_draft | `return type` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the return type obligation named before work proceeds.                           |
| 0215  | [runtime type](#rune-0215)             | authored   | generated_draft | `runtime type` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the runtime type obligation named before work proceeds.                         |
| 0216  | [scope {Lang}](#rune-0216)             | authored   | generated_draft | `scope` in its Lang sense is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the scope obligation named before work proceeds.                     |
| 0217  | [sealed](#rune-0217)                   | authored   | generated_draft | `sealed` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the sealed obligation named before work proceeds.                                     |
| 0218  | [semantics](#rune-0218)                | authored   | reviewed        | What a program means, not just how it is spelled.                                                                                                                                                                              |
| 0219  | [signature {Lang}](#rune-0219)         | authored   | generated_draft | `signature` in its Lang sense is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the signature obligation named before work proceeds.             |
| 0220  | [source code](#rune-0220)              | authored   | generated_draft | `source code` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the source code obligation named before work proceeds.                           |
| 0221  | [specialization](#rune-0221)           | authored   | generated_draft | `specialization` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the specialization obligation named before work proceeds.                     |
| 0222  | [statement](#rune-0222)                | authored   | generated_draft | `statement` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the statement obligation named before work proceeds.                               |
| 0223  | [static dispatch](#rune-0223)          | authored   | generated_draft | `static dispatch` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the static dispatch obligation named before work proceeds.                   |
| 0224  | [strictness](#rune-0224)               | authored   | generated_draft | `strictness` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the strictness obligation named before work proceeds.                             |
| 0225  | [struct {Lang}](#rune-0225)            | authored   | generated_draft | `struct` in its Lang sense is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the struct obligation named before work proceeds.                   |
| 0226  | [subtype](#rune-0226)                  | authored   | generated_draft | `subtype` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the subtype obligation named before work proceeds.                                   |
| 0227  | [supertype](#rune-0227)                | authored   | generated_draft | `supertype` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the supertype obligation named before work proceeds.                               |
| 0228  | [symbol](#rune-0228)                   | authored   | generated_draft | `symbol` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the symbol obligation named before work proceeds.                                     |
| 0229  | [syntax](#rune-0229)                   | authored   | reviewed        | The legal surface form of the language.                                                                                                                                                                                        |
| 0230  | [template {Lang}](#rune-0230)          | authored   | generated_draft | `template` in its Lang sense is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the template obligation named before work proceeds.               |
| 0231  | [theorem {Lang}](#rune-0231)           | authored   | generated_draft | `theorem` in its Lang sense is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the theorem obligation named before work proceeds.                 |
| 0232  | [token {Lang}](#rune-0232)             | authored   | generated_draft | `token` in its Lang sense is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the token obligation named before work proceeds.                     |
| 0233  | [trait](#rune-0233)                    | authored   | generated_draft | `trait` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the trait obligation named before work proceeds.                                       |
| 0234  | [transpiler](#rune-0234)               | authored   | generated_draft | `transpiler` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the transpiler obligation named before work proceeds.                             |
| 0235  | [tuple {Lang}](#rune-0235)             | authored   | generated_draft | `tuple` in its Lang sense is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the tuple obligation named before work proceeds.                     |
| 0236  | [type](#rune-0236)                     | authored   | reviewed        | A declared or inferred class of values and allowed operations.                                                                                                                                                                 |
| 0237  | [type alias](#rune-0237)               | authored   | generated_draft | `type alias` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the type alias obligation named before work proceeds.                             |
| 0238  | [type inference](#rune-0238)           | authored   | generated_draft | `type inference` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the type inference obligation named before work proceeds.                     |
| 0239  | [type system](#rune-0239)              | authored   | generated_draft | `type system` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the type system obligation named before work proceeds.                           |
| 0240  | [typechecker](#rune-0240)              | authored   | reviewed        | The checker that enforces type law before execution.                                                                                                                                                                           |
| 0241  | [typeclass](#rune-0241)                | authored   | generated_draft | `typeclass` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the typeclass obligation named before work proceeds.                               |
| 0242  | [unification](#rune-0242)              | authored   | generated_draft | `unification` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the unification obligation named before work proceeds.                           |
| 0243  | [union](#rune-0243)                    | authored   | generated_draft | `union` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the union obligation named before work proceeds.                                       |
| 0244  | [value](#rune-0244)                    | authored   | generated_draft | `value` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the value obligation named before work proceeds.                                       |
| 0245  | [variance](#rune-0245)                 | authored   | generated_draft | `variance` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the variance obligation named before work proceeds.                                 |
| 0246  | [virtual](#rune-0246)                  | authored   | generated_draft | `virtual` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the virtual obligation named before work proceeds.                                   |
| 0247  | [visibility](#rune-0247)               | authored   | generated_draft | `visibility` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the visibility obligation named before work proceeds.                             |
| 0248  | [visitor](#rune-0248)                  | authored   | generated_draft | `visitor` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the visitor obligation named before work proceeds.                                   |
| 0249  | [where-clause](#rune-0249)             | authored   | generated_draft | `where-clause` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the where-clause obligation named before work proceeds.                         |

## Entries

### [0113] abstract class {#rune-0113}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `abstract class` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the abstract class obligation named before work proceeds.

**Shadow:** misusing `abstract class` at rune 0113 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `abstract class` when the prompt needs this obligation made explicit: `abstract class` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the abstract class obligation named before work proceeds.
- Ask the assistant to state the `abstract class` shadow before proposing changes.

**Example clause:**

Before editing, state the `abstract class` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0114] alias {#rune-0114}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `alias` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the alias obligation named before work proceeds.

**Shadow:** misusing `alias` at rune 0114 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `alias` when the prompt needs this obligation made explicit: `alias` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the alias obligation named before work proceeds.
- Ask the assistant to state the `alias` shadow before proposing changes.

**Example clause:**

Before editing, state the `alias` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0115] annotation {#rune-0115}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `annotation` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the annotation obligation named before work proceeds.

**Shadow:** misusing `annotation` at rune 0115 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `annotation` when the prompt needs this obligation made explicit: `annotation` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the annotation obligation named before work proceeds.
- Ask the assistant to state the `annotation` shadow before proposing changes.

**Example clause:**

Before editing, state the `annotation` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0116] argument {#rune-0116}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `argument` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the argument obligation named before work proceeds.

**Shadow:** misusing `argument` at rune 0116 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `argument` when the prompt needs this obligation made explicit: `argument` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the argument obligation named before work proceeds.
- Ask the assistant to state the `argument` shadow before proposing changes.

**Example clause:**

Before editing, state the `argument` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0117] arity {#rune-0117}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `arity` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the arity obligation named before work proceeds.

**Shadow:** misusing `arity` at rune 0117 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `arity` when the prompt needs this obligation made explicit: `arity` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the arity obligation named before work proceeds.
- Ask the assistant to state the `arity` shadow before proposing changes.

**Example clause:**

Before editing, state the `arity` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0118] assembly {#rune-0118}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `assembly` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the assembly obligation named before work proceeds.

**Shadow:** misusing `assembly` at rune 0118 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `assembly` when the prompt needs this obligation made explicit: `assembly` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the assembly obligation named before work proceeds.
- Ask the assistant to state the `assembly` shadow before proposing changes.

**Example clause:**

Before editing, state the `assembly` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0119] assertion {Lang} {#rune-0119}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `assertion` in its Lang sense is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the assertion obligation named before work proceeds.

**Shadow:** misusing `assertion` at rune 0119 in the Lang lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `assertion` when the prompt needs this obligation made explicit: `assertion` in its Lang sense is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the assertion obligation named before work proceeds.
- Ask the assistant to state the `assertion` shadow before proposing changes.

**Example clause:**

Before editing, state the `assertion` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0120] AST {#rune-0120}

**Status:** canonical (pocket canon)

**Completion:** authored

**Semantic status:** reviewed

**Force:** The tree form of parsed program structure.

**Shadow:** misusing `AST` at rune 0120 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `AST` when the prompt needs this obligation made explicit: The tree form of parsed program structure.
- Ask the assistant to state the `AST` shadow before proposing changes.

**Example clause:**

Before editing, state the `AST` obligation, the evidence that satisfies it, and the failure mode if it is missing.

**Pocket gloss:** The tree form of parsed program structure.

### [0121] attribute {#rune-0121}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `attribute` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the attribute obligation named before work proceeds.

**Shadow:** misusing `attribute` at rune 0121 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `attribute` when the prompt needs this obligation made explicit: `attribute` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the attribute obligation named before work proceeds.
- Ask the assistant to state the `attribute` shadow before proposing changes.

**Example clause:**

Before editing, state the `attribute` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0122] binding {#rune-0122}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `binding` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the binding obligation named before work proceeds.

**Shadow:** misusing `binding` at rune 0122 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `binding` when the prompt needs this obligation made explicit: `binding` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the binding obligation named before work proceeds.
- Ask the assistant to state the `binding` shadow before proposing changes.

**Example clause:**

Before editing, state the `binding` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0123] borrow {Lang} {#rune-0123}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `borrow` in its Lang sense is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the borrow obligation named before work proceeds.

**Shadow:** misusing `borrow` at rune 0123 in the Lang lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `borrow` when the prompt needs this obligation made explicit: `borrow` in its Lang sense is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the borrow obligation named before work proceeds.
- Ask the assistant to state the `borrow` shadow before proposing changes.

**Example clause:**

Before editing, state the `borrow` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0124] borrow checker {#rune-0124}

**Status:** canonical (pocket canon)

**Completion:** authored

**Semantic status:** reviewed

**Force:** A checker that enforces safe borrowing and lifetime law before runtime.

**Shadow:** misusing `borrow checker` at rune 0124 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `borrow checker` when the prompt needs this obligation made explicit: A checker that enforces safe borrowing and lifetime law before runtime.
- Ask the assistant to state the `borrow checker` shadow before proposing changes.

**Example clause:**

Before editing, state the `borrow checker` obligation, the evidence that satisfies it, and the failure mode if it is missing.

**Pocket gloss:** A checker that enforces safe borrowing and lifetime law before runtime.

### [0125] bytecode {#rune-0125}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `bytecode` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the bytecode obligation named before work proceeds.

**Shadow:** misusing `bytecode` at rune 0125 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `bytecode` when the prompt needs this obligation made explicit: `bytecode` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the bytecode obligation named before work proceeds.
- Ask the assistant to state the `bytecode` shadow before proposing changes.

**Example clause:**

Before editing, state the `bytecode` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0126] capture {#rune-0126}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `capture` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the capture obligation named before work proceeds.

**Shadow:** misusing `capture` at rune 0126 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `capture` when the prompt needs this obligation made explicit: `capture` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the capture obligation named before work proceeds.
- Ask the assistant to state the `capture` shadow before proposing changes.

**Example clause:**

Before editing, state the `capture` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0127] cast {#rune-0127}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `cast` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the cast obligation named before work proceeds.

**Shadow:** misusing `cast` at rune 0127 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `cast` when the prompt needs this obligation made explicit: `cast` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the cast obligation named before work proceeds.
- Ask the assistant to state the `cast` shadow before proposing changes.

**Example clause:**

Before editing, state the `cast` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0128] class {#rune-0128}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `class` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the class obligation named before work proceeds.

**Shadow:** misusing `class` at rune 0128 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `class` when the prompt needs this obligation made explicit: `class` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the class obligation named before work proceeds.
- Ask the assistant to state the `class` shadow before proposing changes.

**Example clause:**

Before editing, state the `class` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0129] closure {#rune-0129}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `closure` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the closure obligation named before work proceeds.

**Shadow:** misusing `closure` at rune 0129 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `closure` when the prompt needs this obligation made explicit: `closure` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the closure obligation named before work proceeds.
- Ask the assistant to state the `closure` shadow before proposing changes.

**Example clause:**

Before editing, state the `closure` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0130] coercion {#rune-0130}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `coercion` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the coercion obligation named before work proceeds.

**Shadow:** misusing `coercion` at rune 0130 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `coercion` when the prompt needs this obligation made explicit: `coercion` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the coercion obligation named before work proceeds.
- Ask the assistant to state the `coercion` shadow before proposing changes.

**Example clause:**

Before editing, state the `coercion` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0131] comment {#rune-0131}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `comment` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the comment obligation named before work proceeds.

**Shadow:** misusing `comment` at rune 0131 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `comment` when the prompt needs this obligation made explicit: `comment` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the comment obligation named before work proceeds.
- Ask the assistant to state the `comment` shadow before proposing changes.

**Example clause:**

Before editing, state the `comment` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0132] compile-time {#rune-0132}

**Status:** canonical (pocket canon)

**Completion:** authored

**Semantic status:** reviewed

**Force:** Known and fixed before execution begins.

**Shadow:** misusing `compile-time` at rune 0132 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `compile-time` when the prompt needs this obligation made explicit: Known and fixed before execution begins.
- Ask the assistant to state the `compile-time` shadow before proposing changes.

**Example clause:**

Before editing, state the `compile-time` obligation, the evidence that satisfies it, and the failure mode if it is missing.

**Pocket gloss:** Known and fixed before execution begins.

### [0133] compiler {#rune-0133}

**Status:** canonical (pocket canon)

**Completion:** authored

**Semantic status:** reviewed

**Force:** The translator from source language to lower-level executable form.

**Shadow:** misusing `compiler` at rune 0133 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `compiler` when the prompt needs this obligation made explicit: The translator from source language to lower-level executable form.
- Ask the assistant to state the `compiler` shadow before proposing changes.

**Example clause:**

Before editing, state the `compiler` obligation, the evidence that satisfies it, and the failure mode if it is missing.

**Pocket gloss:** The translator from source language to lower-level executable form.

### [0134] constant {#rune-0134}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `constant` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the constant obligation named before work proceeds.

**Shadow:** misusing `constant` at rune 0134 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `constant` when the prompt needs this obligation made explicit: `constant` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the constant obligation named before work proceeds.
- Ask the assistant to state the `constant` shadow before proposing changes.

**Example clause:**

Before editing, state the `constant` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0135] constraint {Lang} {#rune-0135}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `constraint` in its Lang sense is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the constraint obligation named before work proceeds.

**Shadow:** misusing `constraint` at rune 0135 in the Lang lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `constraint` when the prompt needs this obligation made explicit: `constraint` in its Lang sense is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the constraint obligation named before work proceeds.
- Ask the assistant to state the `constraint` shadow before proposing changes.

**Example clause:**

Before editing, state the `constraint` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0136] constructor {#rune-0136}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `constructor` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the constructor obligation named before work proceeds.

**Shadow:** misusing `constructor` at rune 0136 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `constructor` when the prompt needs this obligation made explicit: `constructor` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the constructor obligation named before work proceeds.
- Ask the assistant to state the `constructor` shadow before proposing changes.

**Example clause:**

Before editing, state the `constructor` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0137] contravariance {#rune-0137}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `contravariance` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the contravariance obligation named before work proceeds.

**Shadow:** misusing `contravariance` at rune 0137 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `contravariance` when the prompt needs this obligation made explicit: `contravariance` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the contravariance obligation named before work proceeds.
- Ask the assistant to state the `contravariance` shadow before proposing changes.

**Example clause:**

Before editing, state the `contravariance` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0138] covariance {#rune-0138}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `covariance` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the covariance obligation named before work proceeds.

**Shadow:** misusing `covariance` at rune 0138 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `covariance` when the prompt needs this obligation made explicit: `covariance` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the covariance obligation named before work proceeds.
- Ask the assistant to state the `covariance` shadow before proposing changes.

**Example clause:**

Before editing, state the `covariance` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0139] currying {#rune-0139}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `currying` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the currying obligation named before work proceeds.

**Shadow:** misusing `currying` at rune 0139 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `currying` when the prompt needs this obligation made explicit: `currying` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the currying obligation named before work proceeds.
- Ask the assistant to state the `currying` shadow before proposing changes.

**Example clause:**

Before editing, state the `currying` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0140] declaration {#rune-0140}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `declaration` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the declaration obligation named before work proceeds.

**Shadow:** misusing `declaration` at rune 0140 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `declaration` when the prompt needs this obligation made explicit: `declaration` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the declaration obligation named before work proceeds.
- Ask the assistant to state the `declaration` shadow before proposing changes.

**Example clause:**

Before editing, state the `declaration` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0141] decorator {Lang} {#rune-0141}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `decorator` in its Lang sense is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the decorator obligation named before work proceeds.

**Shadow:** misusing `decorator` at rune 0141 in the Lang lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `decorator` when the prompt needs this obligation made explicit: `decorator` in its Lang sense is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the decorator obligation named before work proceeds.
- Ask the assistant to state the `decorator` shadow before proposing changes.

**Example clause:**

Before editing, state the `decorator` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0142] default {#rune-0142}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `default` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the default obligation named before work proceeds.

**Shadow:** misusing `default` at rune 0142 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `default` when the prompt needs this obligation made explicit: `default` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the default obligation named before work proceeds.
- Ask the assistant to state the `default` shadow before proposing changes.

**Example clause:**

Before editing, state the `default` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0143] definition {#rune-0143}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `definition` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the definition obligation named before work proceeds.

**Shadow:** misusing `definition` at rune 0143 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `definition` when the prompt needs this obligation made explicit: `definition` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the definition obligation named before work proceeds.
- Ask the assistant to state the `definition` shadow before proposing changes.

**Example clause:**

Before editing, state the `definition` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0144] denotation {#rune-0144}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `denotation` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the denotation obligation named before work proceeds.

**Shadow:** misusing `denotation` at rune 0144 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `denotation` when the prompt needs this obligation made explicit: `denotation` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the denotation obligation named before work proceeds.
- Ask the assistant to state the `denotation` shadow before proposing changes.

**Example clause:**

Before editing, state the `denotation` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0145] destructor {Lang} {#rune-0145}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `destructor` in its Lang sense is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the destructor obligation named before work proceeds.

**Shadow:** misusing `destructor` at rune 0145 in the Lang lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `destructor` when the prompt needs this obligation made explicit: `destructor` in its Lang sense is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the destructor obligation named before work proceeds.
- Ask the assistant to state the `destructor` shadow before proposing changes.

**Example clause:**

Before editing, state the `destructor` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0146] dispatch {Lang} {#rune-0146}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `dispatch` in its Lang sense is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the dispatch obligation named before work proceeds.

**Shadow:** misusing `dispatch` at rune 0146 in the Lang lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `dispatch` when the prompt needs this obligation made explicit: `dispatch` in its Lang sense is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the dispatch obligation named before work proceeds.
- Ask the assistant to state the `dispatch` shadow before proposing changes.

**Example clause:**

Before editing, state the `dispatch` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0147] domain-specific language {#rune-0147}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `domain-specific language` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the domain-specific language obligation named before work proceeds.

**Shadow:** misusing `domain-specific language` at rune 0147 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `domain-specific language` when the prompt needs this obligation made explicit: `domain-specific language` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the domain-specific language obligation named before work proceeds.
- Ask the assistant to state the `domain-specific language` shadow before proposing changes.

**Example clause:**

Before editing, state the `domain-specific language` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0148] dynamic scope {#rune-0148}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `dynamic scope` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the dynamic scope obligation named before work proceeds.

**Shadow:** misusing `dynamic scope` at rune 0148 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `dynamic scope` when the prompt needs this obligation made explicit: `dynamic scope` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the dynamic scope obligation named before work proceeds.
- Ask the assistant to state the `dynamic scope` shadow before proposing changes.

**Example clause:**

Before editing, state the `dynamic scope` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0149] effect {#rune-0149}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `effect` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the effect obligation named before work proceeds.

**Shadow:** misusing `effect` at rune 0149 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `effect` when the prompt needs this obligation made explicit: `effect` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the effect obligation named before work proceeds.
- Ask the assistant to state the `effect` shadow before proposing changes.

**Example clause:**

Before editing, state the `effect` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0150] enum {#rune-0150}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `enum` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the enum obligation named before work proceeds.

**Shadow:** misusing `enum` at rune 0150 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `enum` when the prompt needs this obligation made explicit: `enum` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the enum obligation named before work proceeds.
- Ask the assistant to state the `enum` shadow before proposing changes.

**Example clause:**

Before editing, state the `enum` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0151] evaluation {Lang} {#rune-0151}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `evaluation` in its Lang sense is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the evaluation obligation named before work proceeds.

**Shadow:** misusing `evaluation` at rune 0151 in the Lang lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `evaluation` when the prompt needs this obligation made explicit: `evaluation` in its Lang sense is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the evaluation obligation named before work proceeds.
- Ask the assistant to state the `evaluation` shadow before proposing changes.

**Example clause:**

Before editing, state the `evaluation` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0152] export {Lang} {#rune-0152}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `export` in its Lang sense is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the export obligation named before work proceeds.

**Shadow:** misusing `export` at rune 0152 in the Lang lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `export` when the prompt needs this obligation made explicit: `export` in its Lang sense is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the export obligation named before work proceeds.
- Ask the assistant to state the `export` shadow before proposing changes.

**Example clause:**

Before editing, state the `export` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0153] expression {#rune-0153}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `expression` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the expression obligation named before work proceeds.

**Shadow:** misusing `expression` at rune 0153 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `expression` when the prompt needs this obligation made explicit: `expression` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the expression obligation named before work proceeds.
- Ask the assistant to state the `expression` shadow before proposing changes.

**Example clause:**

Before editing, state the `expression` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0154] extern {#rune-0154}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `extern` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the extern obligation named before work proceeds.

**Shadow:** misusing `extern` at rune 0154 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `extern` when the prompt needs this obligation made explicit: `extern` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the extern obligation named before work proceeds.
- Ask the assistant to state the `extern` shadow before proposing changes.

**Example clause:**

Before editing, state the `extern` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0155] field {Lang} {#rune-0155}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `field` in its Lang sense is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the field obligation named before work proceeds.

**Shadow:** misusing `field` at rune 0155 in the Lang lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `field` when the prompt needs this obligation made explicit: `field` in its Lang sense is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the field obligation named before work proceeds.
- Ask the assistant to state the `field` shadow before proposing changes.

**Example clause:**

Before editing, state the `field` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0156] final {#rune-0156}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `final` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the final obligation named before work proceeds.

**Shadow:** misusing `final` at rune 0156 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `final` when the prompt needs this obligation made explicit: `final` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the final obligation named before work proceeds.
- Ask the assistant to state the `final` shadow before proposing changes.

**Example clause:**

Before editing, state the `final` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0157] function {#rune-0157}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `function` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the function obligation named before work proceeds.

**Shadow:** misusing `function` at rune 0157 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `function` when the prompt needs this obligation made explicit: `function` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the function obligation named before work proceeds.
- Ask the assistant to state the `function` shadow before proposing changes.

**Example clause:**

Before editing, state the `function` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0158] generic {Lang} {#rune-0158}

**Status:** canonical (pocket canon)

**Completion:** authored

**Semantic status:** reviewed

**Force:** One definition parameterized over many concrete types or cases.

**Shadow:** misusing `generic` at rune 0158 in the Lang lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `generic` when the prompt needs this obligation made explicit: One definition parameterized over many concrete types or cases.
- Ask the assistant to state the `generic` shadow before proposing changes.

**Example clause:**

Before editing, state the `generic` obligation, the evidence that satisfies it, and the failure mode if it is missing.

**Pocket gloss:** One definition parameterized over many concrete types or cases.

### [0159] grammar {#rune-0159}

**Status:** canonical (pocket canon)

**Completion:** authored

**Semantic status:** reviewed

**Force:** The rules that say what strings count as well-formed structure.

**Shadow:** misusing `grammar` at rune 0159 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `grammar` when the prompt needs this obligation made explicit: The rules that say what strings count as well-formed structure.
- Ask the assistant to state the `grammar` shadow before proposing changes.

**Example clause:**

Before editing, state the `grammar` obligation, the evidence that satisfies it, and the failure mode if it is missing.

**Pocket gloss:** The rules that say what strings count as well-formed structure.

### [0160] higher-order function {#rune-0160}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `higher-order function` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the higher-order function obligation named before work proceeds.

**Shadow:** misusing `higher-order function` at rune 0160 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `higher-order function` when the prompt needs this obligation made explicit: `higher-order function` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the higher-order function obligation named before work proceeds.
- Ask the assistant to state the `higher-order function` shadow before proposing changes.

**Example clause:**

Before editing, state the `higher-order function` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0161] identifier {#rune-0161}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `identifier` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the identifier obligation named before work proceeds.

**Shadow:** misusing `identifier` at rune 0161 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `identifier` when the prompt needs this obligation made explicit: `identifier` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the identifier obligation named before work proceeds.
- Ask the assistant to state the `identifier` shadow before proposing changes.

**Example clause:**

Before editing, state the `identifier` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0162] impl {#rune-0162}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `impl` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the impl obligation named before work proceeds.

**Shadow:** misusing `impl` at rune 0162 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `impl` when the prompt needs this obligation made explicit: `impl` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the impl obligation named before work proceeds.
- Ask the assistant to state the `impl` shadow before proposing changes.

**Example clause:**

Before editing, state the `impl` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0163] implementation {Lang} {#rune-0163}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `implementation` in its Lang sense is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the implementation obligation named before work proceeds.

**Shadow:** misusing `implementation` at rune 0163 in the Lang lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `implementation` when the prompt needs this obligation made explicit: `implementation` in its Lang sense is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the implementation obligation named before work proceeds.
- Ask the assistant to state the `implementation` shadow before proposing changes.

**Example clause:**

Before editing, state the `implementation` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0164] import {#rune-0164}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `import` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the import obligation named before work proceeds.

**Shadow:** misusing `import` at rune 0164 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `import` when the prompt needs this obligation made explicit: `import` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the import obligation named before work proceeds.
- Ask the assistant to state the `import` shadow before proposing changes.

**Example clause:**

Before editing, state the `import` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0165] include {#rune-0165}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `include` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the include obligation named before work proceeds.

**Shadow:** misusing `include` at rune 0165 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `include` when the prompt needs this obligation made explicit: `include` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the include obligation named before work proceeds.
- Ask the assistant to state the `include` shadow before proposing changes.

**Example clause:**

Before editing, state the `include` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0166] inference {#rune-0166}

**Status:** canonical (pocket canon)

**Completion:** authored

**Semantic status:** reviewed

**Force:** Recovering missing type or shape information from surrounding evidence.

**Shadow:** misusing `inference` at rune 0166 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `inference` when the prompt needs this obligation made explicit: Recovering missing type or shape information from surrounding evidence.
- Ask the assistant to state the `inference` shadow before proposing changes.

**Example clause:**

Before editing, state the `inference` obligation, the evidence that satisfies it, and the failure mode if it is missing.

**Pocket gloss:** Recovering missing type or shape information from surrounding evidence.

### [0167] inheritance {#rune-0167}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `inheritance` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the inheritance obligation named before work proceeds.

**Shadow:** misusing `inheritance` at rune 0167 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `inheritance` when the prompt needs this obligation made explicit: `inheritance` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the inheritance obligation named before work proceeds.
- Ask the assistant to state the `inheritance` shadow before proposing changes.

**Example clause:**

Before editing, state the `inheritance` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0168] inline {Lang} {#rune-0168}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `inline` in its Lang sense is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the inline obligation named before work proceeds.

**Shadow:** misusing `inline` at rune 0168 in the Lang lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `inline` when the prompt needs this obligation made explicit: `inline` in its Lang sense is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the inline obligation named before work proceeds.
- Ask the assistant to state the `inline` shadow before proposing changes.

**Example clause:**

Before editing, state the `inline` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0169] instruction {Lang} {#rune-0169}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `instruction` in its Lang sense is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the instruction obligation named before work proceeds.

**Shadow:** misusing `instruction` at rune 0169 in the Lang lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `instruction` when the prompt needs this obligation made explicit: `instruction` in its Lang sense is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the instruction obligation named before work proceeds.
- Ask the assistant to state the `instruction` shadow before proposing changes.

**Example clause:**

Before editing, state the `instruction` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0170] interface {Lang} {#rune-0170}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `interface` in its Lang sense is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the interface obligation named before work proceeds.

**Shadow:** misusing `interface` at rune 0170 in the Lang lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `interface` when the prompt needs this obligation made explicit: `interface` in its Lang sense is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the interface obligation named before work proceeds.
- Ask the assistant to state the `interface` shadow before proposing changes.

**Example clause:**

Before editing, state the `interface` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0171] interpreter {Lang} {#rune-0171}

**Status:** canonical (pocket canon)

**Completion:** authored

**Semantic status:** reviewed

**Force:** A runtime that executes a representation rather than precompiling it away.

**Shadow:** misusing `interpreter` at rune 0171 in the Lang lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `interpreter` when the prompt needs this obligation made explicit: A runtime that executes a representation rather than precompiling it away.
- Ask the assistant to state the `interpreter` shadow before proposing changes.

**Example clause:**

Before editing, state the `interpreter` obligation, the evidence that satisfies it, and the failure mode if it is missing.

**Pocket gloss:** A runtime that executes a representation rather than precompiling it away.

### [0172] IR {#rune-0172}

**Status:** canonical (pocket canon)

**Completion:** authored

**Semantic status:** reviewed

**Force:** An internal representation built for analysis and transformation.

**Shadow:** misusing `IR` at rune 0172 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `IR` when the prompt needs this obligation made explicit: An internal representation built for analysis and transformation.
- Ask the assistant to state the `IR` shadow before proposing changes.

**Example clause:**

Before editing, state the `IR` obligation, the evidence that satisfies it, and the failure mode if it is missing.

**Pocket gloss:** An internal representation built for analysis and transformation.

### [0173] keyword {#rune-0173}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `keyword` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the keyword obligation named before work proceeds.

**Shadow:** misusing `keyword` at rune 0173 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `keyword` when the prompt needs this obligation made explicit: `keyword` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the keyword obligation named before work proceeds.
- Ask the assistant to state the `keyword` shadow before proposing changes.

**Example clause:**

Before editing, state the `keyword` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0174] lambda {#rune-0174}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `lambda` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the lambda obligation named before work proceeds.

**Shadow:** misusing `lambda` at rune 0174 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `lambda` when the prompt needs this obligation made explicit: `lambda` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the lambda obligation named before work proceeds.
- Ask the assistant to state the `lambda` shadow before proposing changes.

**Example clause:**

Before editing, state the `lambda` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0175] lexical scope {#rune-0175}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `lexical scope` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the lexical scope obligation named before work proceeds.

**Shadow:** misusing `lexical scope` at rune 0175 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `lexical scope` when the prompt needs this obligation made explicit: `lexical scope` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the lexical scope obligation named before work proceeds.
- Ask the assistant to state the `lexical scope` shadow before proposing changes.

**Example clause:**

Before editing, state the `lexical scope` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0176] lifetime {Lang} {#rune-0176}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `lifetime` in its Lang sense is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the lifetime obligation named before work proceeds.

**Shadow:** misusing `lifetime` at rune 0176 in the Lang lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `lifetime` when the prompt needs this obligation made explicit: `lifetime` in its Lang sense is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the lifetime obligation named before work proceeds.
- Ask the assistant to state the `lifetime` shadow before proposing changes.

**Example clause:**

Before editing, state the `lifetime` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0177] linker {#rune-0177}

**Status:** canonical (pocket canon)

**Completion:** authored

**Semantic status:** reviewed

**Force:** The binder that turns compiled pieces into one loadable whole.

**Shadow:** misusing `linker` at rune 0177 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `linker` when the prompt needs this obligation made explicit: The binder that turns compiled pieces into one loadable whole.
- Ask the assistant to state the `linker` shadow before proposing changes.

**Example clause:**

Before editing, state the `linker` obligation, the evidence that satisfies it, and the failure mode if it is missing.

**Pocket gloss:** The binder that turns compiled pieces into one loadable whole.

### [0178] literal {#rune-0178}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `literal` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the literal obligation named before work proceeds.

**Shadow:** misusing `literal` at rune 0178 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `literal` when the prompt needs this obligation made explicit: `literal` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the literal obligation named before work proceeds.
- Ask the assistant to state the `literal` shadow before proposing changes.

**Example clause:**

Before editing, state the `literal` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0179] loader {#rune-0179}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `loader` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the loader obligation named before work proceeds.

**Shadow:** misusing `loader` at rune 0179 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `loader` when the prompt needs this obligation made explicit: `loader` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the loader obligation named before work proceeds.
- Ask the assistant to state the `loader` shadow before proposing changes.

**Example clause:**

Before editing, state the `loader` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0180] macro {#rune-0180}

**Status:** canonical (pocket canon)

**Completion:** authored

**Semantic status:** reviewed

**Force:** Code that writes or reshapes code.

**Shadow:** misusing `macro` at rune 0180 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `macro` when the prompt needs this obligation made explicit: Code that writes or reshapes code.
- Ask the assistant to state the `macro` shadow before proposing changes.

**Example clause:**

Before editing, state the `macro` obligation, the evidence that satisfies it, and the failure mode if it is missing.

**Pocket gloss:** Code that writes or reshapes code.

### [0181] metaprogramming {#rune-0181}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `metaprogramming` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the metaprogramming obligation named before work proceeds.

**Shadow:** misusing `metaprogramming` at rune 0181 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `metaprogramming` when the prompt needs this obligation made explicit: `metaprogramming` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the metaprogramming obligation named before work proceeds.
- Ask the assistant to state the `metaprogramming` shadow before proposing changes.

**Example clause:**

Before editing, state the `metaprogramming` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0182] method {#rune-0182}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `method` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the method obligation named before work proceeds.

**Shadow:** misusing `method` at rune 0182 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `method` when the prompt needs this obligation made explicit: `method` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the method obligation named before work proceeds.
- Ask the assistant to state the `method` shadow before proposing changes.

**Example clause:**

Before editing, state the `method` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0183] mixin {#rune-0183}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `mixin` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the mixin obligation named before work proceeds.

**Shadow:** misusing `mixin` at rune 0183 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `mixin` when the prompt needs this obligation made explicit: `mixin` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the mixin obligation named before work proceeds.
- Ask the assistant to state the `mixin` shadow before proposing changes.

**Example clause:**

Before editing, state the `mixin` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0184] module {Lang} {#rune-0184}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `module` in its Lang sense is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the module obligation named before work proceeds.

**Shadow:** misusing `module` at rune 0184 in the Lang lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `module` when the prompt needs this obligation made explicit: `module` in its Lang sense is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the module obligation named before work proceeds.
- Ask the assistant to state the `module` shadow before proposing changes.

**Example clause:**

Before editing, state the `module` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0185] monomorphization {#rune-0185}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `monomorphization` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the monomorphization obligation named before work proceeds.

**Shadow:** misusing `monomorphization` at rune 0185 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `monomorphization` when the prompt needs this obligation made explicit: `monomorphization` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the monomorphization obligation named before work proceeds.
- Ask the assistant to state the `monomorphization` shadow before proposing changes.

**Example clause:**

Before editing, state the `monomorphization` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0186] mutability {#rune-0186}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `mutability` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the mutability obligation named before work proceeds.

**Shadow:** misusing `mutability` at rune 0186 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `mutability` when the prompt needs this obligation made explicit: `mutability` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the mutability obligation named before work proceeds.
- Ask the assistant to state the `mutability` shadow before proposing changes.

**Example clause:**

Before editing, state the `mutability` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0187] name resolution {#rune-0187}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `name resolution` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the name resolution obligation named before work proceeds.

**Shadow:** misusing `name resolution` at rune 0187 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `name resolution` when the prompt needs this obligation made explicit: `name resolution` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the name resolution obligation named before work proceeds.
- Ask the assistant to state the `name resolution` shadow before proposing changes.

**Example clause:**

Before editing, state the `name resolution` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0188] namespace {Lang} {#rune-0188}

**Status:** canonical (pocket canon)

**Completion:** authored

**Semantic status:** reviewed

**Force:** A naming region that prevents collisions and clarifies ownership.

**Shadow:** misusing `namespace` at rune 0188 in the Lang lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `namespace` when the prompt needs this obligation made explicit: A naming region that prevents collisions and clarifies ownership.
- Ask the assistant to state the `namespace` shadow before proposing changes.

**Example clause:**

Before editing, state the `namespace` obligation, the evidence that satisfies it, and the failure mode if it is missing.

**Pocket gloss:** A naming region that prevents collisions and clarifies ownership.

### [0189] object {#rune-0189}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `object` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the object obligation named before work proceeds.

**Shadow:** misusing `object` at rune 0189 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `object` when the prompt needs this obligation made explicit: `object` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the object obligation named before work proceeds.
- Ask the assistant to state the `object` shadow before proposing changes.

**Example clause:**

Before editing, state the `object` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0190] opcode {Lang} {#rune-0190}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `opcode` in its Lang sense is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the opcode obligation named before work proceeds.

**Shadow:** misusing `opcode` at rune 0190 in the Lang lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `opcode` when the prompt needs this obligation made explicit: `opcode` in its Lang sense is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the opcode obligation named before work proceeds.
- Ask the assistant to state the `opcode` shadow before proposing changes.

**Example clause:**

Before editing, state the `opcode` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0191] overload {#rune-0191}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `overload` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the overload obligation named before work proceeds.

**Shadow:** misusing `overload` at rune 0191 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `overload` when the prompt needs this obligation made explicit: `overload` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the overload obligation named before work proceeds.
- Ask the assistant to state the `overload` shadow before proposing changes.

**Example clause:**

Before editing, state the `overload` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0192] override {#rune-0192}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `override` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the override obligation named before work proceeds.

**Shadow:** misusing `override` at rune 0192 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `override` when the prompt needs this obligation made explicit: `override` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the override obligation named before work proceeds.
- Ask the assistant to state the `override` shadow before proposing changes.

**Example clause:**

Before editing, state the `override` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0193] ownership {#rune-0193}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `ownership` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the ownership obligation named before work proceeds.

**Shadow:** misusing `ownership` at rune 0193 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `ownership` when the prompt needs this obligation made explicit: `ownership` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the ownership obligation named before work proceeds.
- Ask the assistant to state the `ownership` shadow before proposing changes.

**Example clause:**

Before editing, state the `ownership` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0194] package {Lang} {#rune-0194}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `package` in its Lang sense is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the package obligation named before work proceeds.

**Shadow:** misusing `package` at rune 0194 in the Lang lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `package` when the prompt needs this obligation made explicit: `package` in its Lang sense is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the package obligation named before work proceeds.
- Ask the assistant to state the `package` shadow before proposing changes.

**Example clause:**

Before editing, state the `package` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0195] parameter {#rune-0195}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `parameter` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the parameter obligation named before work proceeds.

**Shadow:** misusing `parameter` at rune 0195 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `parameter` when the prompt needs this obligation made explicit: `parameter` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the parameter obligation named before work proceeds.
- Ask the assistant to state the `parameter` shadow before proposing changes.

**Example clause:**

Before editing, state the `parameter` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0196] parametric polymorphism {#rune-0196}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `parametric polymorphism` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the parametric polymorphism obligation named before work proceeds.

**Shadow:** misusing `parametric polymorphism` at rune 0196 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `parametric polymorphism` when the prompt needs this obligation made explicit: `parametric polymorphism` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the parametric polymorphism obligation named before work proceeds.
- Ask the assistant to state the `parametric polymorphism` shadow before proposing changes.

**Example clause:**

Before editing, state the `parametric polymorphism` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0197] parser {#rune-0197}

**Status:** canonical (pocket canon)

**Completion:** authored

**Semantic status:** reviewed

**Force:** The mechanism that turns surface text into structured meaning.

**Shadow:** misusing `parser` at rune 0197 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `parser` when the prompt needs this obligation made explicit: The mechanism that turns surface text into structured meaning.
- Ask the assistant to state the `parser` shadow before proposing changes.

**Example clause:**

Before editing, state the `parser` obligation, the evidence that satisfies it, and the failure mode if it is missing.

**Pocket gloss:** The mechanism that turns surface text into structured meaning.

### [0198] pattern {#rune-0198}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `pattern` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the pattern obligation named before work proceeds.

**Shadow:** misusing `pattern` at rune 0198 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `pattern` when the prompt needs this obligation made explicit: `pattern` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the pattern obligation named before work proceeds.
- Ask the assistant to state the `pattern` shadow before proposing changes.

**Example clause:**

Before editing, state the `pattern` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0199] pattern matching {#rune-0199}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `pattern matching` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the pattern matching obligation named before work proceeds.

**Shadow:** misusing `pattern matching` at rune 0199 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `pattern matching` when the prompt needs this obligation made explicit: `pattern matching` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the pattern matching obligation named before work proceeds.
- Ask the assistant to state the `pattern matching` shadow before proposing changes.

**Example clause:**

Before editing, state the `pattern matching` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0200] polymorphism {#rune-0200}

**Status:** canonical (pocket canon)

**Completion:** authored

**Semantic status:** reviewed

**Force:** One interface, many concrete behaviors.

**Shadow:** misusing `polymorphism` at rune 0200 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `polymorphism` when the prompt needs this obligation made explicit: One interface, many concrete behaviors.
- Ask the assistant to state the `polymorphism` shadow before proposing changes.

**Example clause:**

Before editing, state the `polymorphism` obligation, the evidence that satisfies it, and the failure mode if it is missing.

**Pocket gloss:** One interface, many concrete behaviors.

### [0201] procedure {Lang} {#rune-0201}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `procedure` in its Lang sense is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the procedure obligation named before work proceeds.

**Shadow:** misusing `procedure` at rune 0201 in the Lang lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `procedure` when the prompt needs this obligation made explicit: `procedure` in its Lang sense is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the procedure obligation named before work proceeds.
- Ask the assistant to state the `procedure` shadow before proposing changes.

**Example clause:**

Before editing, state the `procedure` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0202] program {#rune-0202}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `program` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the program obligation named before work proceeds.

**Shadow:** misusing `program` at rune 0202 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `program` when the prompt needs this obligation made explicit: `program` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the program obligation named before work proceeds.
- Ask the assistant to state the `program` shadow before proposing changes.

**Example clause:**

Before editing, state the `program` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0203] property {Lang} {#rune-0203}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `property` in its Lang sense is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the property obligation named before work proceeds.

**Shadow:** misusing `property` at rune 0203 in the Lang lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `property` when the prompt needs this obligation made explicit: `property` in its Lang sense is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the property obligation named before work proceeds.
- Ask the assistant to state the `property` shadow before proposing changes.

**Example clause:**

Before editing, state the `property` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0204] protected {#rune-0204}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `protected` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the protected obligation named before work proceeds.

**Shadow:** misusing `protected` at rune 0204 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `protected` when the prompt needs this obligation made explicit: `protected` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the protected obligation named before work proceeds.
- Ask the assistant to state the `protected` shadow before proposing changes.

**Example clause:**

Before editing, state the `protected` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0205] public {#rune-0205}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `public` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the public obligation named before work proceeds.

**Shadow:** misusing `public` at rune 0205 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `public` when the prompt needs this obligation made explicit: `public` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the public obligation named before work proceeds.
- Ask the assistant to state the `public` shadow before proposing changes.

**Example clause:**

Before editing, state the `public` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0206] purity {#rune-0206}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `purity` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the purity obligation named before work proceeds.

**Shadow:** misusing `purity` at rune 0206 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `purity` when the prompt needs this obligation made explicit: `purity` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the purity obligation named before work proceeds.
- Ask the assistant to state the `purity` shadow before proposing changes.

**Example clause:**

Before editing, state the `purity` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0207] qualified name {#rune-0207}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `qualified name` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the qualified name obligation named before work proceeds.

**Shadow:** misusing `qualified name` at rune 0207 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `qualified name` when the prompt needs this obligation made explicit: `qualified name` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the qualified name obligation named before work proceeds.
- Ask the assistant to state the `qualified name` shadow before proposing changes.

**Example clause:**

Before editing, state the `qualified name` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0208] readonly {#rune-0208}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `readonly` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the readonly obligation named before work proceeds.

**Shadow:** misusing `readonly` at rune 0208 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `readonly` when the prompt needs this obligation made explicit: `readonly` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the readonly obligation named before work proceeds.
- Ask the assistant to state the `readonly` shadow before proposing changes.

**Example clause:**

Before editing, state the `readonly` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0209] record {Lang} {#rune-0209}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `record` in its Lang sense is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the record obligation named before work proceeds.

**Shadow:** misusing `record` at rune 0209 in the Lang lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `record` when the prompt needs this obligation made explicit: `record` in its Lang sense is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the record obligation named before work proceeds.
- Ask the assistant to state the `record` shadow before proposing changes.

**Example clause:**

Before editing, state the `record` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0210] reference {Lang} {#rune-0210}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `reference` in its Lang sense is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the reference obligation named before work proceeds.

**Shadow:** misusing `reference` at rune 0210 in the Lang lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `reference` when the prompt needs this obligation made explicit: `reference` in its Lang sense is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the reference obligation named before work proceeds.
- Ask the assistant to state the `reference` shadow before proposing changes.

**Example clause:**

Before editing, state the `reference` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0211] referential transparency {#rune-0211}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `referential transparency` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the referential transparency obligation named before work proceeds.

**Shadow:** misusing `referential transparency` at rune 0211 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `referential transparency` when the prompt needs this obligation made explicit: `referential transparency` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the referential transparency obligation named before work proceeds.
- Ask the assistant to state the `referential transparency` shadow before proposing changes.

**Example clause:**

Before editing, state the `referential transparency` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0212] reflection {#rune-0212}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `reflection` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the reflection obligation named before work proceeds.

**Shadow:** misusing `reflection` at rune 0212 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `reflection` when the prompt needs this obligation made explicit: `reflection` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the reflection obligation named before work proceeds.
- Ask the assistant to state the `reflection` shadow before proposing changes.

**Example clause:**

Before editing, state the `reflection` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0213] resolution {#rune-0213}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `resolution` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the resolution obligation named before work proceeds.

**Shadow:** misusing `resolution` at rune 0213 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `resolution` when the prompt needs this obligation made explicit: `resolution` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the resolution obligation named before work proceeds.
- Ask the assistant to state the `resolution` shadow before proposing changes.

**Example clause:**

Before editing, state the `resolution` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0214] return type {#rune-0214}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `return type` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the return type obligation named before work proceeds.

**Shadow:** misusing `return type` at rune 0214 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `return type` when the prompt needs this obligation made explicit: `return type` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the return type obligation named before work proceeds.
- Ask the assistant to state the `return type` shadow before proposing changes.

**Example clause:**

Before editing, state the `return type` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0215] runtime type {#rune-0215}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `runtime type` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the runtime type obligation named before work proceeds.

**Shadow:** misusing `runtime type` at rune 0215 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `runtime type` when the prompt needs this obligation made explicit: `runtime type` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the runtime type obligation named before work proceeds.
- Ask the assistant to state the `runtime type` shadow before proposing changes.

**Example clause:**

Before editing, state the `runtime type` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0216] scope {Lang} {#rune-0216}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `scope` in its Lang sense is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the scope obligation named before work proceeds.

**Shadow:** misusing `scope` at rune 0216 in the Lang lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `scope` when the prompt needs this obligation made explicit: `scope` in its Lang sense is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the scope obligation named before work proceeds.
- Ask the assistant to state the `scope` shadow before proposing changes.

**Example clause:**

Before editing, state the `scope` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0217] sealed {#rune-0217}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `sealed` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the sealed obligation named before work proceeds.

**Shadow:** misusing `sealed` at rune 0217 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `sealed` when the prompt needs this obligation made explicit: `sealed` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the sealed obligation named before work proceeds.
- Ask the assistant to state the `sealed` shadow before proposing changes.

**Example clause:**

Before editing, state the `sealed` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0218] semantics {#rune-0218}

**Status:** canonical (pocket canon)

**Completion:** authored

**Semantic status:** reviewed

**Force:** What a program means, not just how it is spelled.

**Shadow:** misusing `semantics` at rune 0218 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `semantics` when the prompt needs this obligation made explicit: What a program means, not just how it is spelled.
- Ask the assistant to state the `semantics` shadow before proposing changes.

**Example clause:**

Before editing, state the `semantics` obligation, the evidence that satisfies it, and the failure mode if it is missing.

**Pocket gloss:** What a program means, not just how it is spelled.

### [0219] signature {Lang} {#rune-0219}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `signature` in its Lang sense is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the signature obligation named before work proceeds.

**Shadow:** misusing `signature` at rune 0219 in the Lang lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `signature` when the prompt needs this obligation made explicit: `signature` in its Lang sense is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the signature obligation named before work proceeds.
- Ask the assistant to state the `signature` shadow before proposing changes.

**Example clause:**

Before editing, state the `signature` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0220] source code {#rune-0220}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `source code` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the source code obligation named before work proceeds.

**Shadow:** misusing `source code` at rune 0220 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `source code` when the prompt needs this obligation made explicit: `source code` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the source code obligation named before work proceeds.
- Ask the assistant to state the `source code` shadow before proposing changes.

**Example clause:**

Before editing, state the `source code` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0221] specialization {#rune-0221}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `specialization` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the specialization obligation named before work proceeds.

**Shadow:** misusing `specialization` at rune 0221 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `specialization` when the prompt needs this obligation made explicit: `specialization` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the specialization obligation named before work proceeds.
- Ask the assistant to state the `specialization` shadow before proposing changes.

**Example clause:**

Before editing, state the `specialization` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0222] statement {#rune-0222}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `statement` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the statement obligation named before work proceeds.

**Shadow:** misusing `statement` at rune 0222 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `statement` when the prompt needs this obligation made explicit: `statement` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the statement obligation named before work proceeds.
- Ask the assistant to state the `statement` shadow before proposing changes.

**Example clause:**

Before editing, state the `statement` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0223] static dispatch {#rune-0223}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `static dispatch` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the static dispatch obligation named before work proceeds.

**Shadow:** misusing `static dispatch` at rune 0223 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `static dispatch` when the prompt needs this obligation made explicit: `static dispatch` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the static dispatch obligation named before work proceeds.
- Ask the assistant to state the `static dispatch` shadow before proposing changes.

**Example clause:**

Before editing, state the `static dispatch` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0224] strictness {#rune-0224}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `strictness` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the strictness obligation named before work proceeds.

**Shadow:** misusing `strictness` at rune 0224 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `strictness` when the prompt needs this obligation made explicit: `strictness` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the strictness obligation named before work proceeds.
- Ask the assistant to state the `strictness` shadow before proposing changes.

**Example clause:**

Before editing, state the `strictness` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0225] struct {Lang} {#rune-0225}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `struct` in its Lang sense is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the struct obligation named before work proceeds.

**Shadow:** misusing `struct` at rune 0225 in the Lang lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `struct` when the prompt needs this obligation made explicit: `struct` in its Lang sense is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the struct obligation named before work proceeds.
- Ask the assistant to state the `struct` shadow before proposing changes.

**Example clause:**

Before editing, state the `struct` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0226] subtype {#rune-0226}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `subtype` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the subtype obligation named before work proceeds.

**Shadow:** misusing `subtype` at rune 0226 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `subtype` when the prompt needs this obligation made explicit: `subtype` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the subtype obligation named before work proceeds.
- Ask the assistant to state the `subtype` shadow before proposing changes.

**Example clause:**

Before editing, state the `subtype` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0227] supertype {#rune-0227}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `supertype` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the supertype obligation named before work proceeds.

**Shadow:** misusing `supertype` at rune 0227 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `supertype` when the prompt needs this obligation made explicit: `supertype` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the supertype obligation named before work proceeds.
- Ask the assistant to state the `supertype` shadow before proposing changes.

**Example clause:**

Before editing, state the `supertype` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0228] symbol {#rune-0228}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `symbol` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the symbol obligation named before work proceeds.

**Shadow:** misusing `symbol` at rune 0228 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `symbol` when the prompt needs this obligation made explicit: `symbol` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the symbol obligation named before work proceeds.
- Ask the assistant to state the `symbol` shadow before proposing changes.

**Example clause:**

Before editing, state the `symbol` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0229] syntax {#rune-0229}

**Status:** canonical (pocket canon)

**Completion:** authored

**Semantic status:** reviewed

**Force:** The legal surface form of the language.

**Shadow:** misusing `syntax` at rune 0229 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `syntax` when the prompt needs this obligation made explicit: The legal surface form of the language.
- Ask the assistant to state the `syntax` shadow before proposing changes.

**Example clause:**

Before editing, state the `syntax` obligation, the evidence that satisfies it, and the failure mode if it is missing.

**Pocket gloss:** The legal surface form of the language.

### [0230] template {Lang} {#rune-0230}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `template` in its Lang sense is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the template obligation named before work proceeds.

**Shadow:** misusing `template` at rune 0230 in the Lang lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `template` when the prompt needs this obligation made explicit: `template` in its Lang sense is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the template obligation named before work proceeds.
- Ask the assistant to state the `template` shadow before proposing changes.

**Example clause:**

Before editing, state the `template` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0231] theorem {Lang} {#rune-0231}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `theorem` in its Lang sense is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the theorem obligation named before work proceeds.

**Shadow:** misusing `theorem` at rune 0231 in the Lang lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `theorem` when the prompt needs this obligation made explicit: `theorem` in its Lang sense is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the theorem obligation named before work proceeds.
- Ask the assistant to state the `theorem` shadow before proposing changes.

**Example clause:**

Before editing, state the `theorem` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0232] token {Lang} {#rune-0232}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `token` in its Lang sense is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the token obligation named before work proceeds.

**Shadow:** misusing `token` at rune 0232 in the Lang lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `token` when the prompt needs this obligation made explicit: `token` in its Lang sense is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the token obligation named before work proceeds.
- Ask the assistant to state the `token` shadow before proposing changes.

**Example clause:**

Before editing, state the `token` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0233] trait {#rune-0233}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `trait` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the trait obligation named before work proceeds.

**Shadow:** misusing `trait` at rune 0233 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `trait` when the prompt needs this obligation made explicit: `trait` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the trait obligation named before work proceeds.
- Ask the assistant to state the `trait` shadow before proposing changes.

**Example clause:**

Before editing, state the `trait` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0234] transpiler {#rune-0234}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `transpiler` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the transpiler obligation named before work proceeds.

**Shadow:** misusing `transpiler` at rune 0234 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `transpiler` when the prompt needs this obligation made explicit: `transpiler` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the transpiler obligation named before work proceeds.
- Ask the assistant to state the `transpiler` shadow before proposing changes.

**Example clause:**

Before editing, state the `transpiler` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0235] tuple {Lang} {#rune-0235}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `tuple` in its Lang sense is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the tuple obligation named before work proceeds.

**Shadow:** misusing `tuple` at rune 0235 in the Lang lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `tuple` when the prompt needs this obligation made explicit: `tuple` in its Lang sense is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the tuple obligation named before work proceeds.
- Ask the assistant to state the `tuple` shadow before proposing changes.

**Example clause:**

Before editing, state the `tuple` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0236] type {#rune-0236}

**Status:** canonical (pocket canon)

**Completion:** authored

**Semantic status:** reviewed

**Force:** A declared or inferred class of values and allowed operations.

**Shadow:** misusing `type` at rune 0236 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `type` when the prompt needs this obligation made explicit: A declared or inferred class of values and allowed operations.
- Ask the assistant to state the `type` shadow before proposing changes.

**Example clause:**

Before editing, state the `type` obligation, the evidence that satisfies it, and the failure mode if it is missing.

**Pocket gloss:** A declared or inferred class of values and allowed operations.

### [0237] type alias {#rune-0237}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `type alias` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the type alias obligation named before work proceeds.

**Shadow:** misusing `type alias` at rune 0237 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `type alias` when the prompt needs this obligation made explicit: `type alias` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the type alias obligation named before work proceeds.
- Ask the assistant to state the `type alias` shadow before proposing changes.

**Example clause:**

Before editing, state the `type alias` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0238] type inference {#rune-0238}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `type inference` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the type inference obligation named before work proceeds.

**Shadow:** misusing `type inference` at rune 0238 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `type inference` when the prompt needs this obligation made explicit: `type inference` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the type inference obligation named before work proceeds.
- Ask the assistant to state the `type inference` shadow before proposing changes.

**Example clause:**

Before editing, state the `type inference` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0239] type system {#rune-0239}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `type system` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the type system obligation named before work proceeds.

**Shadow:** misusing `type system` at rune 0239 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `type system` when the prompt needs this obligation made explicit: `type system` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the type system obligation named before work proceeds.
- Ask the assistant to state the `type system` shadow before proposing changes.

**Example clause:**

Before editing, state the `type system` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0240] typechecker {#rune-0240}

**Status:** canonical (pocket canon)

**Completion:** authored

**Semantic status:** reviewed

**Force:** The checker that enforces type law before execution.

**Shadow:** misusing `typechecker` at rune 0240 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `typechecker` when the prompt needs this obligation made explicit: The checker that enforces type law before execution.
- Ask the assistant to state the `typechecker` shadow before proposing changes.

**Example clause:**

Before editing, state the `typechecker` obligation, the evidence that satisfies it, and the failure mode if it is missing.

**Pocket gloss:** The checker that enforces type law before execution.

### [0241] typeclass {#rune-0241}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `typeclass` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the typeclass obligation named before work proceeds.

**Shadow:** misusing `typeclass` at rune 0241 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `typeclass` when the prompt needs this obligation made explicit: `typeclass` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the typeclass obligation named before work proceeds.
- Ask the assistant to state the `typeclass` shadow before proposing changes.

**Example clause:**

Before editing, state the `typeclass` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0242] unification {#rune-0242}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `unification` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the unification obligation named before work proceeds.

**Shadow:** misusing `unification` at rune 0242 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `unification` when the prompt needs this obligation made explicit: `unification` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the unification obligation named before work proceeds.
- Ask the assistant to state the `unification` shadow before proposing changes.

**Example clause:**

Before editing, state the `unification` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0243] union {#rune-0243}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `union` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the union obligation named before work proceeds.

**Shadow:** misusing `union` at rune 0243 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `union` when the prompt needs this obligation made explicit: `union` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the union obligation named before work proceeds.
- Ask the assistant to state the `union` shadow before proposing changes.

**Example clause:**

Before editing, state the `union` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0244] value {#rune-0244}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `value` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the value obligation named before work proceeds.

**Shadow:** misusing `value` at rune 0244 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `value` when the prompt needs this obligation made explicit: `value` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the value obligation named before work proceeds.
- Ask the assistant to state the `value` shadow before proposing changes.

**Example clause:**

Before editing, state the `value` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0245] variance {#rune-0245}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `variance` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the variance obligation named before work proceeds.

**Shadow:** misusing `variance` at rune 0245 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `variance` when the prompt needs this obligation made explicit: `variance` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the variance obligation named before work proceeds.
- Ask the assistant to state the `variance` shadow before proposing changes.

**Example clause:**

Before editing, state the `variance` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0246] virtual {#rune-0246}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `virtual` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the virtual obligation named before work proceeds.

**Shadow:** misusing `virtual` at rune 0246 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `virtual` when the prompt needs this obligation made explicit: `virtual` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the virtual obligation named before work proceeds.
- Ask the assistant to state the `virtual` shadow before proposing changes.

**Example clause:**

Before editing, state the `virtual` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0247] visibility {#rune-0247}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `visibility` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the visibility obligation named before work proceeds.

**Shadow:** misusing `visibility` at rune 0247 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `visibility` when the prompt needs this obligation made explicit: `visibility` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the visibility obligation named before work proceeds.
- Ask the assistant to state the `visibility` shadow before proposing changes.

**Example clause:**

Before editing, state the `visibility` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0248] visitor {#rune-0248}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `visitor` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the visitor obligation named before work proceeds.

**Shadow:** misusing `visitor` at rune 0248 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `visitor` when the prompt needs this obligation made explicit: `visitor` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the visitor obligation named before work proceeds.
- Ask the assistant to state the `visitor` shadow before proposing changes.

**Example clause:**

Before editing, state the `visitor` obligation, the evidence that satisfies it, and the failure mode if it is missing.

### [0249] where-clause {#rune-0249}

**Status:** canonical

**Completion:** authored

**Semantic status:** generated_draft

**Force:** `where-clause` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the where-clause obligation named before work proceeds.

**Shadow:** misusing `where-clause` at rune 0249 in the language lane can let valid-looking form can still carry the wrong meaning; verify by requiring the caster to state the parser, grammar, or semantic rule being relied on.

**Prompt uses:**

- Name `where-clause` when the prompt needs this obligation made explicit: `where-clause` is a language and semantics rune for make meaning, syntax, and interpretation rules inspectable; use it when the artifact needs the where-clause obligation named before work proceeds.
- Ask the assistant to state the `where-clause` shadow before proposing changes.

**Example clause:**

Before editing, state the `where-clause` obligation, the evidence that satisfies it, and the failure mode if it is missing.
